# CODEFEST AD ASTRA 2026 — laka_codefest
# Dependencias del pipeline de extracción/limpieza (src/f1_limpieza_extraccion.py, f2_limpieza_extraccion.py).
# Instalar en un entorno virtual: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- Obligatorias: sin esto los scripts no arrancan ---
pandas==3.0.5
openpyxl==3.1.5          # motor de pd.read_excel para el índice .xlsx
pymupdf==1.28.0           # import fitz — extracción de texto de PDF

# --- Opcionales: cada una degrada una función puntual si falta (ver ADVERTENCIA al importar) ---
langdetect==1.0.9         # detección de idioma; sin ella, idioma_detectado="desconocido"
pytesseract==0.3.13       # OCR (PDFs escaneados e imágenes sueltas); requiere el binario `tesseract` del sistema
pdf2image==1.17.0         # rasteriza páginas de PDF para el fallback de OCR; requiere `poppler-utils` del sistema
pillow==12.3.0            # apertura/decodificación de imágenes para extract_imagen (solo F2+)
pillow-avif-plugin==1.6.0 # decodificador AVIF para Pillow (un único archivo .avif en el corpus de F2)
